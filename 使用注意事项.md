# io_pdx_mesh for Blender 使用注意事项

做 PDX 模型与动画时值得对照的几条，都是实际踩过的坑。

## 导出动画勾选 chk_uniform_scale

让缩放每骨只写 1 个分量。3 分量是 CK3 的格式，引擎按每骨 1 个值读，文件里写了 3 个就会读错位，表现为动画后段模型突然大小抽搐。

## 骨架用唯一骨名

原版有些骨架存在重名骨（如 `star_eater` 的 `cube1` 到 `cube4` 各挂 3 个父级），Blender 会强制改名加 `.001` 后缀。自家骨架请在建模阶段就保证骨名唯一，导入后看到 `.001` 后缀就是撞上了。

改原版重名骨模型时，确认重名骨匹配逻辑已生效。重名骨会同时影响动画采样流、蒙皮权重与挂点父变换，动画那条不报错，很难发现。

## 在空文件里导入带骨架的模型

插件判断「骨架是否已经存在」时，只要传入骨架的**任意一根骨名**能在场景里找到，就认定骨架已存在并直接复用那个骨架。`root`、`main_body` 这类名字在原版模型里高度重叠，所以在已打开其他工程文件的情况下导入，很容易撞上。

撞上之后不报错，但后果是静默的：新模型不会建骨架，顶点组会被写成场景里那个旧骨架的骨名，Armature 修改器也指向旧骨架，网格基本不会跟着动画动。

导入带骨架的模型前先开一个新的空文件，导入完再合并回工程。判断是否撞上，看导入后场景里有没有多出一个新骨架；顶点组的骨名是不是这个模型自己的。

顺带一提，插件的建骨用的是 `bpy.ops.object.mode_set`，需要 3D 视口上下文。界面里手工导入没问题，用脚本在后台无窗口调用会报 `上下文缺失活动物体`，需要自己补 `temp_override`。

## 贴图不在 .mesh 旁边是正常的

`.mesh` 里存的是贴图文件名，文件本身不保证和 `.mesh` 放在一起。原版就有大量这种安排，例如 `gfx/models/ships/starbases/` 下的空间站模型，贴图实际在 `gfx/models/ships/<风格>/` 目录里。

本分叉已按「先找 `.mesh` 同目录，再回退到该资产根 `gfx` 目录下按文件名搜索」的方式解析。若仍然出现粉红或纯白贴图，先确认这张 `.dds` 在整个游戏 `gfx` 目录里到底存不存在——原版确实有个别模型引用了根本不存在的贴图（如 `toxoid_01_ion_cannon.mesh` 引用的 `toxic_01_*` 系列）。

## 先导入网格，再导入动画

动画里的骨名骨序必须和场景骨架对应，顺序颠倒会报 `Missing unique armature`。

## 整体位移放在有蒙皮权重的躯干骨上

引擎的蒙皮不吃 root 骨的平移，位移加在 root 上网格不动。加在 root 的子骨上，兄弟骨各加一份。

位移必须落进每帧位移采样流。只改 info 段初始位移、或写成 Blender 里恒定的 location 关键帧，都会被导出器压掉，游戏里不生效。

## 动画 fps 用整数

原版常见 15.06 这类非整数。自家动画直接用 15、30、60。

## 动画时长基本不是异常来源

原版最长有 10002 帧的文件，超 900 帧的有 158 个。排查「动画太长导致异常」之前先排除这个。

## 游戏里表现异常时，先查映射链

最后才怀疑动画数据本身。映射链是 entity 的 state、`.gfx` 里的 animation 映射、`.asset` 末尾指向具体 `.anim` 三处。

`.asset` 与 `.gfx` 都是整文件替换，mod 里放同名文件会把原版其余定义一起顶掉，症状像数据损坏但根因不同。

去 `logs\error.log` 搜 `pdxmeshtype`、`pdx_entity` 和实体名。

规避做法是不用与原版同名的定义，用自有前缀，这样与原版零覆盖，也不会与其他 mod 互顶。

## 坐标系换算

Blender 的 Z 轴对应模型的 Y 轴。要让东西在游戏里竖直向上，位移写在 Blender 的 +Z 方向。

## 导出 .mesh 时留意骨架姿势

上游 0.91.0 只要导出时骨架不在静止姿态，挂点就会被那个姿势带偏。本分叉已修好。

用上游版本的话，导出前 `rig.data.pose_position = 'REST'`，导出完设回 `'POSE'`。注意跟预览是反的，预览和渲染必须留在 `'POSE'`。

## 不能用「看起来正常」当通过标准

Blender 里正常不等于游戏里正常。首尾帧相同不等于数据正确。文件体积变小是正常的，导出器会压缩恒定通道。

判断动画对不对要比姿态数据、用四元数差角，不能只看「顺不顺」。
