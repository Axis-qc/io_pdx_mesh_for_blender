# io_pdx_mesh for Blender 使用注意事项

做 PDX 模型与动画时值得对照的几条。都是实际踩过的坑，不是推测。

## 导出动画必须勾选 chk_uniform_scale

让缩放每骨只写 1 个分量。原版 2698 个 `.anim` 文件里，每根骨的初始缩放向量长度全是 1，用 3 分量布局的文件数为 0。其中 705 个带缩放动画的文件，缩放采样数 705/705 都等于「骨数 × 帧数 × 1」。

3 分量是 CK3 的格式，插件源码注释里标了 `[CK3]`。引擎按每骨 1 个值读，若文件里写了 3 个，读指针每帧只前进 1/3，缩放值整体错位。表现为动画前段看起来正常、后段模型突然大小抽搐，因为前段那些缩放值还是 1.0。

## 骨架一律使用唯一骨名

原版模型会出现重名骨，例如 `star_eater` 的 `cube1` 到 `cube4` 各挂 3 个父级，18 根骨只有 10 个唯一名。Blender 强制骨名唯一，导入时自动改名并加 `.001`、`.002` 后缀。

自家新做的骨架请在建模阶段就保证骨名唯一。判据是导入后骨名带 `.001` 后缀就说明撞上了重名骨。

改原版重名骨模型时，确认重名骨匹配逻辑已生效。重名骨会同时影响动画采样流、蒙皮权重与挂点父变换三处，且动画那条因为「少消费」不越界，不会报错，很难发现。

## 先导入网格，再导入动画

动画文件里的骨名与骨序必须和场景里的骨架对应。顺序颠倒会报 `Missing unique armature`。

重名骨被加后缀之后这一点尤其重要，因为骨架里实际存在的骨名是加过后缀的那个。

## 动画时长不是异常来源

原版 `base_star_idle.anim` 有 10002 帧、约 666 秒，超过 900 帧的原版文件有 158 个。排查「动画太长导致异常」之前先排除这个，基本不是原因。

## 整体位移要放在有蒙皮权重的躯干骨上

不要移动物体本身，也不要只加在 root 骨上。引擎的蒙皮不吃 root 骨的平移，把位移加在 root 上，游戏里只有挂在骨上的东西跟着动，网格不动。整体位移要加在 root 的子骨上，兄弟骨各加一份，它们子树不相交不会翻倍。

位移还必须落进 `.anim` 的每帧位移采样流。只改 info 段的初始位移，或写成 Blender 里恒定的 location 关键帧，都会被导出器压成「该骨没有位移通道」加「初始位移变了」，Blender 里看着是沉的，游戏里不生效。

## 动画 fps 用整数

原版常见非整数，如 15.06、15.10。插件只能把场景 fps 设成整数，精确值记录在骨架属性 `io_pdx_anim_fps` 上，导出时若场景 fps 没被改过就还原原值。自家动画直接用 15、30、60 最省心。

## 导入导出后在游戏里表现异常时，先查映射链

最后才怀疑动画数据本身。映射链有三层，缺一不可：entity 的 state、`.gfx` 里 pdxmesh 的 animation 映射、`.asset` 末尾把 animation id 指到具体 `.anim` 文件。

`.asset` 与 `.gfx` 都是同名文件整文件替换，不是按 entity 合并。mod 里放一个同名 `.asset`，会把原版该文件里其余 entity 与文件末尾的 animation 注册一起顶掉，症状与动画数据损坏很像但根因完全不同。

排查入口在 `%USERPROFILE%\Documents\Paradox Interactive\Stellaris\logs\error.log`，搜 `pdxmeshtype`、`pdx_entity` 以及实体名。任一层断裂会依次出现 `Failed to find animation` 与 `does not have an animated mesh`，该 entity 的全部状态（含默认 idle）动画失效。

推荐的规避做法是完全独立，不使用与原版同名的定义：用自有前缀定义 entity、pdxmesh 与 animation 注册，只有资源文件（`.mesh`、`.dds`、`.anim`）可以复用原版，再把 mod 自己的 section_templates 与 ship_sizes 指向自有 entity。这样与原版零覆盖，也不会与其他 mod 互顶。

## 坐标系换算

Blender 的 Z 轴对应模型的 Y 轴，因为 PDX 是 Y-up。插件内部把 `(x, z, y)` 对调，等价于 PDX 的 `(bl_x, bl_z, bl_y)`。

要让东西在游戏里竖直向上，位移必须写在 Blender 的 +Z 方向，写成 +Y 会在水平面内横飞。

## 不能用「看起来正常」当通过标准

Blender 里正常不等于游戏里正常。非均匀缩放的文件在 Blender 里往返完全正常，插件自洽，只有引擎里才错。

首尾帧相同不等于数据正确，整份数据一致偏移时首尾帧照样相同。文件体积明显变小约等于损坏，因为导出器会把恒定通道压缩掉，静止动画体积明显更小是正常的。

判断动画对不对要比姿态数据、用四元数差角，不能用欧拉角直接比，也不能只看「顺不顺」。
